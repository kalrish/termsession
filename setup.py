import setuptools


setuptools.setup(
    name='termsession',
    version='0.1',
    packages=[
        'termsession',
    ],
    install_requires=[
    ],
    entry_points={
        'console_scripts': [
        ],
    },
)
